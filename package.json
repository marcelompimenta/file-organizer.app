{
  "name": "type_classifier",
  "version": "0.0.1",
  "description": "type classifier program, can select and classify the files found in a directory into folders.",
  "main": "app.js",
  "author": "marcelo pimenta (peppe-r)",
  "license": "true",
  "private": false,
  "devDependencies": {
    "@types/node": "^18.15.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "build": "tsc",
    "start": "node build/app.js",
    "dev":" yarn build && yarn start && ts-node-dev --ignore-watch node_modules src/app.ts"
  }
}
